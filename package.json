{
  "name": "layit-flexbox",
  "version": "0.0.1",
  "description": "A smart task runner",
  "main": "dist/lib/main.js",
  "types": "dist/lib/main.d.ts",
  "author": "Mgen (https://www.mgenware.com)",
  "homepage": "https://github.com/mgenware/makhulu",
  "bugs": "https://github.com/mgenware/makhulu/issues",
  "keywords": [
    "task runner"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mgenware/makhulu.git"
  },
  "license": "MIT",
  "scripts": {
    "test": "yarn run clean && yarn run compile && yarn run lint && yarn run dev_test",
    "dev_test": "yarn run copy && jest",
    "build": "yarn test",
    "compile": "tsc",
    "dev": "yarn run clean && tsc -w",
    "prepublishOnly": "yarn run test",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "copy": "copyfiles \"tests/data/**/*.*\" \"dist\"",
    "clean:dist": "rimraf dist",
    "clean": "npm run clean:dist"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^22.2.2",
    "@types/node": "^9.6.0",
    "copyfiles": "^2.0.0",
    "jest": "^22.4.3",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}
